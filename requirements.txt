# Core (required)
yt-dlp>=2024.8.6
youtube-transcript-api>=0.6.2
pydantic>=2.6
rich>=13.7
typer>=0.12
PyYAML>=6.0

# Local web UI (optional entry point: python -m src.webapp)
flask>=3.0

# Optional local speech-to-text fallback (Priority 3).
# The CLI works without these installed; it will explain what is missing
# if audio transcription is actually needed and these are absent.
faster-whisper>=1.0.0
psutil>=5.9

# Documents module: PDF -> Markdown, merge, split (see README.md,
# section "Document Processing"). PyMuPDF is used under the AGPL-3.0
# license.
pymupdf>=1.24

# Optional local OCR fallback for scanned PDFs. Works without these
# installed as long as OCR is not actually needed (documents already
# have a text layer); a clear error explains what's missing otherwise.
# Also requires the Tesseract binary on PATH (not a Python package):
#   winget install --id UB-Mannheim.TesseractOCR
pytesseract>=0.3.10
pillow>=10.0
